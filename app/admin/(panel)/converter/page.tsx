// File Converter section (Req 33.1, 34).
// Renders the full FileConverterPanel for upload, conversion, and preview.
import { FileConverterPanel } from "@/components/admin/converter/FileConverterPanel";

export default function ConverterPage() {
  return <FileConverterPanel />;
}
