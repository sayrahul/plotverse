// GeoJSON History section (Req 33.1, 35). The GeojsonHistoryPanel is built in
// task 17.4; this placeholder keeps the route navigable today.
import { SectionPlaceholder } from "@/components/admin/SectionPlaceholder";

export default function GeojsonHistoryPage() {
  return (
    <SectionPlaceholder
      title="GeoJSON History"
      description="Review saved versions and roll back."
    />
  );
}
